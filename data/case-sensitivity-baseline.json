{
  "$comment": "Rules whose regex means something broader than written, because src/engine.ts compiles every pattern with the 'i' flag. Frozen debt; the gate blocks growth, not the existing population.",
  "generated": "2026-08-23",
  "rules": []
}
