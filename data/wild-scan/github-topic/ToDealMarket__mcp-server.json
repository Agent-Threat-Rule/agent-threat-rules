{
  "full_name": "ToDealMarket/mcp-server",
  "html_url": "https://github.com/ToDealMarket/mcp-server",
  "description": "MCP server for TDM payment integration. Enables AI agents to handle payments through Model Context Protocol.",
  "topics": [
    "ai-agents",
    "anthropic",
    "claude",
    "developer-tools",
    "mcp",
    "mcp-servers",
    "model-context-protocol",
    "payments",
    "typescript"
  ],
  "matched_query_topics": [
    "mcp-servers"
  ],
  "stars": 11,
  "forks": 0,
  "open_issues": 0,
  "language": "TypeScript",
  "license": "MIT",
  "default_branch": "main",
  "created_at": "2026-03-18T13:43:05Z",
  "last_pushed_at": "2026-04-20T12:22:28Z",
  "fetched_at": "2026-07-11T23:56:09.361Z",
  "readme": {
    "path": "README.md",
    "content": "<div align=\"center\">\n\n# TDM MCP Server\n\n<img src=\"https://img.shields.io/badge/TDM-MCP_Server-ff69b4?style=for-the-badge&logo=server&logoColor=white\" alt=\"TDM MCP Server\" />\n\n**Model Context Protocol Server for TDM**\n\n[![npm](https://img.shields.io/badge/npm-@tdm/mcp--server-ff69b4?style=flat-square&logo=npm&logoColor=white)](https://www.npmjs.com/package/@tdm/mcp-server)\n[![MCP Compatible](https://img.shields.io/badge/MCP-Compatible-ff69b4?style=flat-square)](https://modelcontextprotocol.io/)\n[![License: MIT](https://img.shields.io/badge/License-MIT-ff69b4?style=flat-square)](./LICENSE)\n[![GitHub stars](https://img.shields.io/github/stars/ToDealMarket/mcp-server?style=flat-square&color=ff69b4)](https://github.com/ToDealMarket/mcp-server/stargazers)\n[![TypeScript](https://img.shields.io/badge/TypeScript-5.0+-ff69b4?style=flat-square&logo=typescript&logoColor=white)](https://www.typescriptlang.org/)\n\n*Session-state checks for AI agents*\n\n[Documentation](https://todealmarket.com/docs) • [Quick Start](https://todealmarket.com/docs/quick-start) • [GitHub](https://github.com/todealmarket) • [X/Twitter](https://x.com/todealmarket)\n\n</div>\n\n---\n\n## What is TDM MCP Server?\n\n`@tdm/mcp-server` is a small MCP stdio server for TDM local session-state checks.\n\nThe public GitHub repo for `tdm-sdk` shows the open contract-facing SDK\nsurface: https://github.com/ToDealMarket/tdm-sdk\n\nThe current `tdm-sdk` npm beta used by the surrounding TDM docs remains\nbroader and includes the CLI/operator flows referenced here.\n\nInside the broader Integration Kit, this package is the reference MCP implementation.\nIt sits next to:\n\n- thin CLI and signer wrappers\n- copy-paste framework examples\n- lightweight local integration recipes\n\n```\n████████╗ ██████╗  ███╗   ███╗\n╚══██╔══╝ ██╔══██╗ ████╗ ████║\n   ██║    ██║  ██║ ██╔████╔██║\n   ██║    ██║  ██║ ██║╚██╔╝██║\n   ██║    ██████╔╝ ██║ ╚═╝ ██║\n   ╚═╝    ╚═════╝  ╚═╝     ╚═╝\n\nTDM CLI [V0.0.2-BETA]\nSDK + CLI + MCP\nMode: local-first | Docs: todealmarket.com/docs\n```\n\n---\n\n## What it helps with\n\nThe tool helps an agent decide whether it should:\n\n- continue\n- ask the user to run `tdm connect`\n- suggest `tdm fuel`\n- suggest `tdm sweep`\n\nInteractive seller commands in `tdm-sdk` can now auto-start that one-time\nLive TDM setup when the live runtime is still missing. MCP still keeps the\nexplicit instruction path because an agent should ask the operator before\ntriggering wallet/browser setup on their behalf.\n\nFor human operator flows, `tdm connect` stores the primary wallet locally.\nThe current multi-wallet MVP also supports connecting one wallet per supported\nnetwork for local signing and network-specific payout setup. During connect,\nTDM can attempt a safe default payout sync for the same network when that\nnetwork does not already have a saved payout wallet. It does not overwrite an\nalready saved payout wallet automatically, and\n`tdm connect --no-sync-payout-wallet` keeps connect local-only.\n\nIf the account protects new payout destinations with TOTP step-up, the\noperator should run:\n\n```bash\ntdm security totp enroll\ntdm security totp verify --method-id <id> --code 123456\n```\n\n`tdm security totp enroll` opens a local setup page with a QR by default. Use\n`--no-browser` if terminal-only setup is preferred.\n\nIt is intentionally local-first:\n\n- reads local TDM credentials and runtime hints\n- follows `TDM_CREDENTIALS_PATH` when explicitly set\n- otherwise follows `TDM_VAULT` or the active vault marker from `~/.tdm/vaults/_active`\n- does not depend on deprecated treasury balance routes\n- can use `TDM_REMAINING_USDC` and `TDM_DUST_TOKENS_DETECTED` when the host wants to provide fresh snapshot hints\n\nFor operators using named vaults, MCP will read the active/default vault context instead of pretending there is only one global runtime forever.\n\nThe vault resolution order is explicit:\n\n- `TDM_CREDENTIALS_PATH` when you pin one credentials file directly\n- otherwise `TDM_VAULT`\n- otherwise the active vault marker from `~/.tdm/vaults/_active`\n- otherwise the legacy default `~/.tdm/credentials.json`\n\nThat means one MCP process reads one selected vault contour. It does not merge\nagents, wallets, or sessions across multiple vaults.\n\nIf that active vault has its own optional runtime allowlist, agents in that\nvault inherit the narrower destination policy. If no project or vault\nallowlist is configured, MCP keeps the broader flexible runtime posture\ninstead of assuming deny-by-default.\n\nIf the operator wants reusable runtime contours, `tdm workspace use <name>`\ncan switch the active vault/storage combination before MCP starts. MCP still\nreads local runtime state only; it does not own those runtime workspaces.\n\nLocal catalogs and published assets now live next to that same runtime layer in `tdm-sdk`:\n\n- `tdm storage add`\n- `tdm storage import-dir`\n- `tdm storage sync`\n- `tdm storage watch`\n- `tdm storage publish`\n- `tdm workspace`\n- `tdm status`\n- `tdm workspace status`\n\nMCP itself does not host or upload those files. It can only help an agent or\noperator decide when to suggest the surrounding CLI workflow.\n\n## States\n\n- `FUELED`\n- `DEPLETED`\n- `UNINITIALIZED`\n\nRepresentative response fields:\n\n- `state`\n- `remaining_usdc`\n- `wallet_linked`\n- `dust_tokens_detected`\n- `agent_instructions`\n- `balance_source`\n\n## Install\n\n```bash\nnpm install @tdm/mcp-server\n```\n\nGlobal install:\n\n```bash\nnpm install -g @tdm/mcp-server\n```\n\nIf you already use `tdm-sdk`, the simplest path is:\n\n```bash\ntdm mcp\n```\n\nThat SDK command writes a project `.mcp.json` pointing to the bundled MCP runtime.\nThe standalone `@tdm/mcp-server` package itself does not mutate your project config files.\n\n## Run\n\n```bash\ntdm-mcp-server\n```\n\nBundled SDK path:\n\n```bash\ntdm mcp serve\n```\n\n## Programmatic startup\n\n```ts\nimport { startUltraThinRadarServer } from '@tdm/mcp-server';\n\nawait startUltraThinRadarServer();\n```\n\n## Example response\n\n```json\n{\n  \"state\": \"DEPLETED\",\n  \"remaining_usdc\": 0,\n  \"wallet_linked\": true,\n  \"dust_tokens_detected\": true,\n  \"agent_instructions\": \"Suggest tdm sweep.\",\n  \"balance_source\": \"runtime_snapshot\"\n}\n```\n\n## Scope\n\nThis package is a decision helper for agent runtimes. It is not a wallet manager,\npayout tool, or general execution framework.\n\nIf you need a live JavaScript gateway integration instead of local state\nguidance, use `tdm-sdk` with `makePayable(...)`, `tdm-sdk/authorize`,\n`tdm-sdk/checkout`, `tdm-sdk/session-tanks`, or `createGatewayClients(...)`.\n\nIf MCP suggests a recovery path such as `tdm sweep`, the current SDK routing\nbehavior behind that suggestion is:\n\n- Solana Jupiter:\n  - `https://api.jup.ag/swap/v1` when `JUPITER_API_KEY` is configured\n  - `https://lite-api.jup.ag/swap/v1` as the compatibility fallback without a key\n- Base Odos:\n  - `https://api.odos.xyz` without `ODOS_API_KEY`\n  - `https://enterprise-api.odos.xyz` with `ODOS_API_KEY`\n\nThe MCP server itself does not hold or manage those API keys; it only points\noperators toward the surrounding `tdm-sdk` workflow.\n\nIf you need custom local signing for advanced third-party integrations, use the\nexperimental `tdm signer serve` mode from `tdm-sdk` instead of treating the MCP\nserver as a transaction executor.\n\nWarning:\n\n- advanced users only\n- localhost-only signing surface\n- blind signing risk exists\n- TDM does not take responsibility for losses caused by compromised local scripts or unsafe third-party integrations in that mode\n\n## License\n\nMIT\n"
  },
  "descriptor_file": null,
  "entrypoint_file": {
    "path": "dist/index.cjs",
    "size": 26505,
    "content": "#!/usr/bin/env node\n'use strict';\n\nvar promises = require('fs/promises');\nvar os = require('os');\nvar path = require('path');\nvar url = require('url');\nvar server = require('@modelcontextprotocol/sdk/server');\nvar stdio_js = require('@modelcontextprotocol/sdk/server/stdio.js');\nvar types_js = require('@modelcontextprotocol/sdk/types.js');\nvar crypto = require('crypto');\n\nvar _documentCurrentScript = typeof document !== 'undefined' ? document.currentScript : null;\n// src/stdio-idle-guard.ts\nvar DEFAULT_HEARTBEAT_MS = 3e4;\nvar DEFAULT_MAX_HISTORY_ENTRIES = 256;\nvar DEFAULT_MAX_ENTRY_LENGTH = 512;\nvar MAX_CAPTURE_LABEL_LENGTH = 64;\nvar FALLBACK_CAPTURE_LABEL = \"event\";\nvar getErrorMessage = (error) => error instanceof Error ? error.message : String(error);\nvar coercePositiveInteger = (value, fallback) => Number.isInteger(value) && value !== void 0 && value > 0 ? value : fallback;\nvar attachStreamErrorListener = (stream, streamName, onError) => {\n  const handleError = (error) => {\n    onError(`${streamName}:error:${error.message}`);\n  };\n  stream.on(\"error\", handleError);\n  return () => {\n    stream.off(\"error\", handleError);\n  };\n};\nvar StdioIdleGuard = class {\n  history;\n  maxHistoryEntries;\n  maxEntryLength;\n  onHeartbeat;\n  heartbeatTimer;\n  cleanups = [];\n  historyStart = 0;\n  historySize = 0;\n  disposed = false;\n  constructor(options = {}) {\n    this.maxHistoryEntries = coercePositiveInteger(\n      options.maxHistoryEntries,\n      DEFAULT_MAX_HISTORY_ENTRIES\n    );\n    this.maxEntryLength = coercePositiveInteger(\n      options.maxEntryLength,\n      DEFAULT_MAX_ENTRY_LENGTH\n    );\n    this.onHeartbeat = options.onHeartbeat;\n    this.history = Array.from({\n      length: this.maxHistoryEntries\n    });\n    const heartbeatMs = coercePositiveInteger(options.heartbeatMs, DEFAULT_HEARTBEAT_MS);\n    this.cleanups.push(\n      attachStreamErrorListener(process.stdin, \"stdin\", (entry) => this.push(entry)),\n      attachStreamErrorListener(process.stdout, \"stdout\", (entry) => this.push(entry)),\n      attachStreamErrorListener(process.stderr, \"stderr\", (entry) => this.push(entry))\n    );\n    this.heartbeatTimer = setInterval(() => {\n      const usage = process.memoryUsage();\n      try {\n        this.onHeartbeat?.({\n          timestampMs: Date.now(),\n          rssBytes: usage.rss,\n          heapUsedBytes: usage.heapUsed,\n          historyDepth: this.historySize\n        });\n      } catch (error) {\n        this.push(`heartbeat:error:${getErrorMessage(error)}`);\n      }\n    }, heartbeatMs);\n    this.heartbeatTimer.unref();\n  }\n  capture(label, value) {\n    if (this.disposed) {\n      return;\n    }\n    const normalizedLabel = label.trim().slice(0, MAX_CAPTURE_LABEL_LENGTH) || FALLBACK_CAPTURE_LABEL;\n    const normalizedValue = value.replace(/\\s+/g, \" \").trim();\n    this.push(`${normalizedLabel}:${normalizedValue}`);\n  }\n  getHistory() {\n    const snapshot = [];\n    for (let offset = 0; offset < this.historySize; offset += 1) {\n      const index = (this.historyStart + offset) % this.maxHistoryEntries;\n      const entry = this.history[index];\n      if (entry !== void 0) {\n        snapshot.push(entry);\n      }\n    }\n    return snapshot;\n  }\n  dispose() {\n    if (this.disposed) {\n      return;\n    }\n    this.disposed = true;\n    clearInterval(this.heartbeatTimer);\n    for (const cleanup of this.cleanups.splice(0)) {\n      cleanup();\n    }\n    this.history.fill(void 0);\n    this.historyStart = 0;\n    this.historySize = 0;\n  }\n  push(entry) {\n    if (this.disposed) {\n      return;\n    }\n    const nextEntry = entry.slice(0, this.maxEntryLength);\n    if (this.historySize < this.maxHistoryEntries) {\n      const writeIndex = (this.historyStart + this.historySize) % this.maxHistoryEntries;\n      this.history[writeIndex] = nextEntry;\n      this.historySize += 1;\n      return;\n    }\n    this.history[this.historyStart] = nextEntry;\n    this.historyStart = (this.historyStart + 1) % this.maxHistoryEntries;\n  }\n};\nfunction attachGracefulShutdown(options) {\n  const signals = Array.from(\n    new Set(options.signals ?? [\"SIGINT\", \"SIGTERM\"])\n  );\n  const handlers = /* @__PURE__ */ new Map();\n  let shutdownPromise = null;\n  const runShutdown = () => {\n    if (shutdownPromise) {\n      return shutdownPromise;\n    }\n    for (const [signal, handler] of handlers) {\n      process.off(signal, handler);\n    }\n    handlers.clear();\n    shutdownPromise = Promise.resolve(options.onShutdown()).then(() => {\n      process.exitCode = 0;\n    }).catch(() => {\n      process.exitCode = 1;\n    });\n    return shutdownPromise;\n  };\n  for (const signal of signals) {\n    const handler = () => {\n      void runShutdown();\n    };\n    handlers.set(signal, handler);\n    process.once(signal, handler);\n  }\n  return () => {\n    for (const [signal, handler] of handlers) {\n      process.off(signal, handler);\n    }\n    handlers.clear();\n  };\n}\n\n// src/json-object.ts\nfunction isJsonObject(value) {\n  return Boolean(value) && typeof value === \"object\" && !Array.isArray(value);\n}\nfunction parseJsonObject(raw) {\n  const parsed = JSON.parse(raw);\n  if (!isJsonObject(parsed)) {\n    throw new TypeError(\"JSON payload must be an object\");\n  }\n  return parsed;\n}\nfunction tryParseJsonObject(raw) {\n  try {\n    return parseJsonObject(raw);\n  } catch {\n    return null;\n  }\n}\nfunction readTrimmedString(value) {\n  if (typeof value !== \"string\") {\n    return void 0;\n  }\n  const trimmed = value.trim();\n  return trimmed.length > 0 ? trimmed : void 0;\n}\nfunction readObjectString(source, key) {\n  return readTrimmedString(source?.[key]);\n}\nvar DEFAULT_CREDENTIALS_PATH = path.join(os.homedir(), \".tdm\", \"credentials.json\");\nvar ENV_KEY_MAP = {\n  gatewayUrl: \"TDM_GATEWAY_URL\",\n  apiKey: \"TDM_API_KEY\",\n  burnerPublicKeyBase58: \"TDM_BURNER_PUBLIC_KEY\",\n  sessionId: \"TDM_SESSION_ID\"\n};\nfunction normalizeCredentialsPath() {\n  const fromEnv = process.env[\"TDM_CREDENTIALS_PATH\"]?.trim();\n  return fromEnv && fromEnv.length > 0 ? fromEnv : DEFAULT_CREDENTIALS_PATH;\n}\nfunction parseEnvContent(content) {\n  const result = {};\n  for (const line of content.split(/\\r?\\n/)) {\n    const trimmed = line.trim();\n    if (!trimmed || trimmed.startsWith(\"#\")) {\n      continue;\n    }\n    const separatorIndex = trimmed.indexOf(\"=\");\n    if (separatorIndex <= 0) {\n      continue;\n    }\n    const key = trimmed.slice(0, separatorIndex).trim();\n    const rawValue = trimmed.slice(separatorIndex + 1).trim();\n    if (!key) {\n      continue;\n    }\n    const quoted = rawValue.startsWith('\"') && rawValue.endsWith('\"') || rawValue.startsWith(\"'\") && rawValue.endsWith(\"'\");\n    result[key] = (quoted ? rawValue.slice(1, -1) : rawValue).replace(/\\\\n/g, \"\\n\");\n  }\n  return result;\n}\nasync function readEnvFile(pathname) {\n  try {\n    return parseEnvContent(await promises.readFile(pathname, \"utf8\"));\n  } catch {\n    return {};\n  }\n}\nasync function readJsonObjectFile(pathname) {\n  try {\n    return tryParseJsonObject(await promises.readFile(pathname, \"utf8\"));\n  } catch {\n    return null;\n  }\n}\nasync function readMetadata(pathname) {\n  return readJsonObjectFile(pathname);\n}\nasync function readSecretBundle(credentialsPath) {\n  const key = crypto.createHash(\"sha256\").update(path.resolve(credentialsPath)).digest(\"hex\");\n  try {\n    const keyring = await import('@napi-rs/keyring');\n    const raw = await new keyring.AsyncEntry(\"local-vault\", key).getPassword();\n    if (!raw) {\n      return null;\n    }\n    return tryParseJsonObject(raw);\n  } catch {\n    return null;\n  }\n}\nasync function readEnvCredentials() {\n  const cwd = process.cwd();\n  const envPath = path.join(cwd, \".env\");\n  const envLocalPath = path.join(cwd, \".env.local\");\n  const [dotenv, dotenvLocal] = await Promise.all([\n    readEnvFile(envPath),\n    readEnvFile(envLocalPath)\n  ]);\n  const merged = {\n    ...dotenv,\n    ...dotenvLocal,\n    ...process.env\n  };\n  return {\n    credentialsPath: normalizeCredentialsPath(),\n    gatewayUrl: readTrimmedString(merged[ENV_KEY_MAP.gatewayUrl]),\n    [REDACTED-GENERIC_KV_SECRET](merged[ENV_KEY_MAP.apiKey]),\n    burnerPublicKeyBase58: readTrimmedString(merged[ENV_KEY_MAP.burnerPublicKeyBase58]),\n    defaultSessionId: readTrimmedString(merged[ENV_KEY_MAP.sessionId])\n  };\n}\nasync function peekRuntimeCredentials() {\n  const credentialsPath = normalizeCredentialsPath();\n  const [envSnapshot, metadata, secrets] = await Promise.all([\n    readEnvCredentials(),\n    readMetadata(credentialsPath),\n    readSecretBundle(credentialsPath)\n  ]);\n  return {\n    credentialsPath,\n    gatewayUrl: envSnapshot.gatewayUrl ?? readObjectString(metadata, \"gatewayUrl\"),\n    apiKey: envSnapshot.apiKey ?? readObjectString(secrets, \"apiKey\"),\n    burnerPublicKeyBase58: envSnapshot.burnerPublicKeyBase58 ?? readObjectString(metadata, \"burnerPublicKeyBase58\"),\n    defaultSessionId: envSnapshot.defaultSessionId ?? readObjectString(metadata, \"defaultSessionId\")\n  };\n}\n\n// src/gateway-balance.ts\nfunction toFiniteNumber(value) {\n  if (typeof value === \"number\" && Number.isFinite(value)) {\n    return value;\n  }\n  if (typeof value === \"string\") {\n    const parsed = Number.parseFloat(value.trim());\n    if (Number.isFinite(parsed)) {\n      return parsed;\n    }\n  }\n  return null;\n}\nfunction toBoolean(value) {\n  if (typeof value === \"boolean\") {\n    return value;\n  }\n  if (typeof value === \"number\") {\n    return value !== 0;\n  }\n  if (typeof value === \"string\") {\n    const normalized = value.trim().toLowerCase();\n    if (normalized === \"true\" || normalized === \"1\" || normalized === \"yes\") {\n      return true;\n    }\n    if (normalized === \"false\" || normalized === \"0\" || normalized === \"no\") {\n      return false;\n    }\n  }\n  return null;\n}\nfunction minorStringToUsd(value) {\n  if (typeof value !== \"string\" || !/^\\d+$/.test(value.trim())) {\n    return null;\n  }\n  const parsed = Number.parseFloat(value.trim());\n  if (!Number.isFinite(parsed)) {\n    return null;\n  }\n  return parsed / 1e6;\n}\nfunction hasPositiveMinorBalance(value) {\n  if (typeof value !== \"string\" || !/^\\d+$/.test(value.trim())) {\n    return false;\n  }\n  try {\n    return BigInt(value.trim()) > 0n;\n  } catch {\n    return false;\n  }\n}\nfunction extractRemainingUsdc(payload) {\n  const derivedFromMinor = minorStringToUsd(payload[\"balanceMinor\"]);\n  const candidates = [\n    payload[\"remaining_usdc\"],\n    payload[\"remainingUsd\"],\n    payload[\"remaining\"],\n    payload[\"balanceUsd\"],\n    payload[\"balanceUSD\"],\n    derivedFromMinor\n  ];\n  for (const candidate of candidates) {\n    const value = toFiniteNumber(candidate);\n    if (value !== null) {\n      return Math.max(0, value);\n    }\n  }\n  return null;\n}\nfunction extractDustTokensDetected(payload) {\n  const candidates = [\n    payload[\"dust_tokens_detected\"],\n    payload[\"dustTokensDetected\"],\n    payload[\"has_dust_tokens\"],\n    payload[\"hasDustTokens\"]\n  ];\n  for (const candidate of candidates) {\n    const value = toBoolean(candidate);\n    if (value !== null) {\n      return value;\n    }\n  }\n  return null;\n}\nfunction extractPositiveBalanceSignal(payload) {\n  if (hasPositiveMinorBalance(payload[\"balanceMinor\"])) {\n    return true;\n  }\n  const probes = [\n    payload[\"balance\"],\n    payload[\"balanceUsd\"],\n    payload[\"balanceUSD\"],\n    payload[\"remaining_usdc\"],\n    payload[\"remainingUsd\"],\n    payload[\"remaining\"]\n  ];\n  for (const probe of probes) {\n    const value = toFiniteNumber(probe);\n    if (value !== null && value > 0) {\n      return true;\n    }\n  }\n  return false;\n}\n\n// src/index.ts\nvar MCP_SERVER_NAME = \"tdm-ultra-thin-radar\";\nvar MCP_SERVER_VERSION = \"1.0.0\";\nvar TOOL_NAME = \"tdm_get_session_state\";\nvar DEBOUNCE_MS = 1e4;\nvar BALANCE_TIMEOUT_MS = 5e3;\nvar DEFAULT_CREDENTIALS_PATH2 = path.join(os.homedir(), \".tdm\", \"credentials.json\");\nvar DEFAULT_DUST_FLAG_PATH = path.join(os.homedir(), \".tdm\", \"dust-cache.json\");\nvar cachedState = null;\nvar inFlightState = null;\nfunction toFiniteNumber2(value) {\n  if (typeof value === \"number\" && Number.isFinite(value)) {\n    return value;\n  }\n  if (typeof value === \"string\") {\n    const parsed = Number.parseFloat(value.trim());\n    if (Number.isFinite(parsed)) {\n      return parsed;\n    }\n  }\n  return null;\n}\nfunction toBoolean2(value) {\n  if (typeof value === \"boolean\") {\n    return value;\n  }\n  if (typeof value === \"number\") {\n    return value !== 0;\n  }\n  if (typeof value === \"string\") {\n    const normalized = value.trim().toLowerCase();\n    if (normalized === \"true\" || normalized === \"1\" || normalized === \"yes\") {\n      return true;\n    }\n    if (normalized === \"false\" || normalized === \"0\" || normalized === \"no\") {\n      return false;\n    }\n  }\n  return null;\n}\nfunction normalizeCredentialsPath2() {\n  const fromEnv = process.env[\"TDM_CREDENTIALS_PATH\"]?.trim();\n  return fromEnv && fromEnv.length > 0 ? fromEnv : DEFAULT_CREDENTIALS_PATH2;\n}\nfunction normalizeDustFlagPath() {\n  const fromEnv = process.env[\"TDM_DUST_FLAG_PATH\"]?.trim();\n  return fromEnv && fromEnv.length > 0 ? fromEnv : DEFAULT_DUST_FLAG_PATH;\n}\nfunction hasRuntimeCredentials(snapshot) {\n  return Boolean(\n    snapshot.gatewayUrl || snapshot.apiKey || snapshot.defaultSessionId || snapshot.burnerPublicKeyBase58\n  );\n}\nasync function readJsonFile(pathname) {\n  try {\n    const raw = await promises.readFile(pathname, \"utf8\");\n    return {\n      status: \"ok\",\n      value: parseJsonObject(raw)\n    };\n  } catch (error) {\n    if (error && typeof error === \"object\" && \"code\" in error && error.code === \"ENOENT\") {\n      return { status: \"missing\" };\n    }\n    if (error instanceof SyntaxError || error instanceof TypeError) {\n      return {\n        status: \"invalid\",\n        message: error.message\n      };\n    }\n    return {\n      status: \"error\",\n      message: error instanceof Error ? error.message : String(error)\n    };\n  }\n}\nasync function readCredentialsSnapshot() {\n  const pathname = normalizeCredentialsPath2();\n  const payloadResult = await readJsonFile(pathname);\n  const runtime = await peekRuntimeCredentials();\n  const runtimeSnapshot = {\n    gatewayUrl: runtime.gatewayUrl,\n    apiKey: runtime.apiKey,\n    defaultSessionId: runtime.defaultSessionId,\n    burnerPublicKeyBase58: runtime.burnerPublicKeyBase58\n  };\n  if (payloadResult.status !== \"ok\") {\n    if (hasRuntimeCredentials(runtimeSnapshot)) {\n      return {\n        status: \"ok\",\n        path: pathname,\n        snapshot: runtimeSnapshot\n      };\n    }\n    if (payloadResult.status === \"missing\") {\n      return {\n        status: \"missing\",\n        path: pathname\n      };\n    }\n    if (payloadResult.status === \"invalid\") {\n      return {\n        status: \"invalid\",\n        path: pathname,\n        message: payloadResult.message\n      };\n    }\n    return {\n      status: \"error\",\n      path: pathname,\n      message: payloadResult.message\n    };\n  }\n  const payload = payloadResult.value;\n  const snapshot = {\n    gatewayUrl: readObjectString(payload, \"gatewayUrl\"),\n    [REDACTED-GENERIC_KV_SECRET](payload, \"apiKey\"),\n    defaultSessionId: readObjectString(payload, \"defaultSessionId\"),\n    burnerPublicKeyBase58: readObjectString(payload, \"burnerPublicKeyBase58\"),\n    dustTokensDetected: toBoolean2(payload[\"dustTokensDetected\"]) ?? void 0,\n    dust_tokens_detected: toBoolean2(payload[\"dust_tokens_detected\"]) ?? void 0,\n    remaining_usdc: toFiniteNumber2(payload[\"remaining_usdc\"]) ?? toFiniteNumber2(payload[\"remainingUsd\"]) ?? toFiniteNumber2(payload[\"remaining\"]) ?? void 0\n  };\n  return {\n    status: \"ok\",\n    path: pathname,\n    snapshot: {\n      ...snapshot,\n      gatewayUrl: runtimeSnapshot.gatewayUrl ?? snapshot.gatewayUrl,\n      apiKey: runtimeSnapshot.apiKey ?? snapshot.apiKey,\n      defaultSessionId: runtimeSnapshot.defaultSessionId ?? snapshot.defaultSessionId,\n      burnerPublicKeyBase58: runtimeSnapshot.burnerPublicKeyBase58 ?? snapshot.burnerPublicKeyBase58\n    }\n  };\n}\nfunction buildBalanceEndpoint(gatewayUrl, sessionId) {\n  const base = new URL(gatewayUrl);\n  const basePath = base.pathname.endsWith(\"/\") ? base.pathname.slice(0, -1) : base.pathname;\n  const hasApiV1 = basePath.endsWith(\"/api/v1\") || basePath === \"/api/v1\";\n  const prefix = hasApiV1 ? basePath : `${basePath}/api/v1`;\n  base.pathname = `${prefix}/treasury/sessions/${encodeURIComponent(sessionId)}/balance`.replace(/\\/{2,}/g, \"/\");\n  base.search = \"\";\n  base.hash = \"\";\n  return base.toString();\n}\nfunction buildAgentInstructions(state, dustTokensDetected) {\n  if (state === \"UNINITIALIZED\") {\n    return \"Inform the human operator to run 'tdm login' in the terminal so credentials are created and linked.\";\n  }\n  if (state === \"DEPLETED\" && !dustTokensDetected) {\n    return \"Inform human execution is halted due to $0.00 funds. Instruct them to run tdm fuel.\";\n  }\n  if (state === \"DEPLETED\" && dustTokensDetected) {\n    return \"Inform human they have low value crypto dust. Suggest they run tdm sweep to auto-convert junk tokens into Agent Fuel natively without external bank accounts.\";\n  }\n  return \"Session fuel is available. Continue x402 execution without requesting additional funding.\";\n}\nasync function resolveDustSignal(credentials, gatewayDustHint) {\n  if (gatewayDustHint !== null) {\n    return gatewayDustHint;\n  }\n  if (typeof credentials.dustTokensDetected === \"boolean\") {\n    return credentials.dustTokensDetected;\n  }\n  if (typeof credentials.dust_tokens_detected === \"boolean\") {\n    return credentials.dust_tokens_detected;\n  }\n  const envFlag = toBoolean2(process.env[\"TDM_DUST_TOKENS_DETECTED\"]);\n  if (envFlag !== null) {\n    return envFlag;\n  }\n  const dustPayload = await readJsonFile(normalizeDustFlagPath());\n  if (dustPayload.status !== \"ok\") {\n    return false;\n  }\n  const payload = dustPayload.value;\n  return toBoolean2(payload[\"dust_tokens_detected\"]) ?? toBoolean2(payload[\"dustTokensDetected\"]) ?? false;\n}\nfunction buildAuthRequiredActionMessage() {\n  return \"Politely ask your human user to run 'tdm login' in the terminal to configure credentials before continuing.\";\n}\nasync function fetchGatewayBalanceSnapshot(credentials) {\n  const fromCreds = credentials.remaining_usdc !== void 0 ? toFiniteNumber2(credentials.remaining_usdc) : null;\n  if (credentials.remaining_usdc !== void 0) {\n    if (fromCreds !== null) {\n      return {\n        remainingUsdc: Math.max(0, fromCreds),\n        dustTokensDetected: null,\n        hasPositiveBalanceSignal: fromCreds > 0\n      };\n    }\n  }\n  if (!credentials.gatewayUrl || !credentials.defaultSessionId) {\n    return {\n      remainingUsdc: 0,\n      dustTokensDetected: null,\n      hasPositiveBalanceSignal: false\n    };\n  }\n  let endpoint;\n  try {\n    endpoint = buildBalanceEndpoint(credentials.gatewayUrl, credentials.defaultSessionId);\n  } catch {\n    return {\n      remainingUsdc: 0,\n      dustTokensDetected: null,\n      hasPositiveBalanceSignal: false\n    };\n  }\n  const headers = new Headers();\n  headers.set(\"accept\", \"application/json\");\n  if (credentials.apiKey && credentials.apiKey.trim().length > 0) {\n    headers.set(\"authorization\", `Bearer ${credentials.apiKey}`);\n    if (credentials.apiKey.startsWith(\"apk_\")) {\n      headers.set(\"x-api-key\", credentials.apiKey);\n    }\n  }\n  const fingerprint = process.env[\"TDM_FINGERPRINT\"]?.trim();\n  if (fingerprint) {\n    headers.set(\"x-fingerprint\", fingerprint);\n  }\n  const controller = new AbortController();\n  const timeout = setTimeout(() => controller.abort(), BALANCE_TIMEOUT_MS);\n  try {\n    const response = await fetch(endpoint, {\n      method: \"GET\",\n      headers,\n      signal: controller.signal\n    });\n    if (!response.ok) {\n      return {\n        remainingUsdc: fromCreds !== null ? Math.max(0, fromCreds) : 0,\n        dustTokensDetected: null,\n        hasPositiveBalanceSignal: fromCreds !== null && fromCreds > 0\n      };\n    }\n    const json = await response.json();\n    if (!json || typeof json !== \"object\" || Array.isArray(json)) {\n      return {\n        remainingUsdc: fromCreds !== null ? Math.max(0, fromCreds) : 0,\n        dustTokensDetected: null,\n        hasPositiveBalanceSignal: fromCreds !== null && fromCreds > 0\n      };\n    }\n    const payload = json;\n    const extractedRemaining = extractRemainingUsdc(payload);\n    const remainingUsdc = extractedRemaining !== null ? Math.max(0, extractedRemaining) : fromCreds !== null ? Math.max(0, fromCreds) : 0;\n    const hasPositiveBalanceSignal = remainingUsdc > 0 || extractPositiveBalanceSignal(payload);\n    return {\n      remainingUsdc,\n      dustTokensDetected: extractDustTokensDetected(payload),\n      hasPositiveBalanceSignal\n    };\n  } catch {\n    return {\n      remainingUsdc: fromCreds !== null ? Math.max(0, fromCreds) : 0,\n      dustTokensDetected: null,\n      hasPositiveBalanceSignal: fromCreds !== null && fromCreds > 0\n    };\n  } finally {\n    clearTimeout(timeout);\n  }\n}\nasync function computeSessionState() {\n  const credentialsResult = await readCredentialsSnapshot();\n  if (credentialsResult.status !== \"ok\") {\n    const authAction = buildAuthRequiredActionMessage();\n    const instructionPrefix = credentialsResult.status === \"missing\" ? \"No local credentials were found.\" : credentialsResult.status === \"invalid\" ? `Local credentials are invalid (${credentialsResult.message}).` : `Local credentials could not be read (${credentialsResult.message}).`;\n    const state2 = {\n      state: \"UNINITIALIZED\",\n      remaining_usdc: 0,\n      wallet_linked: false,\n      dust_tokens_detected: false,\n      agent_instructions: `${instructionPrefix} ${authAction}`,\n      tdm_state: \"AUTH_REQUIRED\",\n      action: authAction,\n      credentials_path: credentialsResult.path\n    };\n    return state2;\n  }\n  const credentials = credentialsResult.snapshot;\n  const gatewayBalance = await fetchGatewayBalanceSnapshot(credentials);\n  const dustSignal = await resolveDustSignal(credentials, gatewayBalance.dustTokensDetected);\n  const walletLinked = typeof credentials.burnerPublicKeyBase58 === \"string\" && credentials.burnerPublicKeyBase58.trim().length > 0;\n  const state = gatewayBalance.remainingUsdc > 0 || gatewayBalance.hasPositiveBalanceSignal ? \"FUELED\" : \"DEPLETED\";\n  return {\n    state,\n    remaining_usdc: Number(gatewayBalance.remainingUsdc.toFixed(2)),\n    wallet_linked: walletLinked,\n    dust_tokens_detected: dustSignal,\n    agent_instructions: buildAgentInstructions(state, dustSignal)\n  };\n}\nasync function getSessionStateWithDebounce() {\n  const now = Date.now();\n  if (cachedState && now - cachedState.fetchedAtMs < DEBOUNCE_MS) {\n    return cachedState.value;\n  }\n  if (inFlightState) {\n    return inFlightState;\n  }\n  inFlightState = computeSessionState().then((value) => {\n    cachedState = {\n      value,\n      fetchedAtMs: Date.now()\n    };\n    return value;\n  }).finally(() => {\n    inFlightState = null;\n  });\n  return inFlightState;\n}\nasync function startUltraThinRadarServer() {\n  const stdioGuard = new StdioIdleGuard({\n    heartbeatMs: Number.parseInt(process.env[\"MCP_STDIO_HEARTBEAT_MS\"] ?? \"30000\", 10),\n    maxHistoryEntries: Number.parseInt(process.env[\"MCP_STDIO_HISTORY_MAX\"] ?? \"256\", 10)\n  });\n  const server$1 = new server.Server(\n    { name: MCP_SERVER_NAME, version: MCP_SERVER_VERSION },\n    { capabilities: { tools: {} } }\n  );\n  server$1.setRequestHandler(types_js.ListToolsRequestSchema, async () => ({\n    tools: [\n      {\n        name: TOOL_NAME,\n        description: \"Provides the LLM Agent with active financial balance and wallet status to determine if x402 payment challenges can be met. Call this BEFORE asking the user for money.\",\n        inputSchema: {\n          type: \"object\",\n          additionalProperties: false,\n          properties: {}\n        }\n      }\n    ]\n  }));\n  server$1.setRequestHandler(types_js.CallToolRequestSchema, async (request) => {\n    const { name, arguments: args } = request.params;\n    if (name !== TOOL_NAME) {\n      return {\n        content: [\n          {\n            type: \"text\",\n            text: JSON.stringify(\n              {\n                error: \"TOOL_NOT_FOUND\",\n                message: `Unknown tool \"${name}\"`\n              },\n              null,\n              2\n            )\n          }\n        ],\n        isError: true\n      };\n    }\n    if (args && typeof args === \"object\" && Object.keys(args).length > 0) {\n      return {\n        content: [\n          {\n            type: \"text\",\n            text: JSON.stringify(\n              {\n                error: \"INVALID_ARGUMENTS\",\n                message: \"tdm_get_session_state does not accept arguments\"\n              },\n              null,\n              2\n            )\n          }\n        ],\n        isError: true\n      };\n    }\n    try {\n      const payload = await getSessionStateWithDebounce();\n      return {\n        content: [\n          {\n            type: \"text\",\n            text: JSON.stringify(payload, null, 2)\n          }\n        ]\n      };\n    } catch (error) {\n      const message = error instanceof Error ? error.message : String(error);\n      stdioGuard.capture(\"tool_error\", message);\n      throw new types_js.McpError(types_js.ErrorCode.InternalError, message);\n    }\n  });\n  const transport = new stdio_js.StdioServerTransport();\n  let detachGracefulShutdown = null;\n  detachGracefulShutdown = attachGracefulShutdown({\n    onShutdown: async () => {\n      detachGracefulShutdown?.();\n      try {\n        if (server$1.transport) {\n          await server$1.close();\n        }\n      } catch (error) {\n        stdioGuard.capture(\"shutdown_error\", error instanceof Error ? error.message : String(error));\n        throw error;\n      } finally {\n        stdioGuard.dispose();\n      }\n    }\n  });\n  try {\n    await server$1.connect(transport);\n  } catch (error) {\n    stdioGuard.capture(\"startup_error\", error instanceof Error ? error.message : String(error));\n    if (server$1.transport) {\n      await server$1.close().catch(() => void 0);\n    }\n    stdioGuard.dispose();\n    detachGracefulShutdown?.();\n    throw error;\n  }\n}\nvar isDirectExecution = process.argv[1] !== void 0 && url.fileURLToPath((typeof document === 'undefined' ? require('u' + 'rl').pathToFileURL(__filename).href : (_documentCurrentScript && _documentCurrentScript.tagName.toUpperCase() === 'SCRIPT' && _documentCurrentScript.src || new URL('index.cjs', document.baseURI).href))) === process.argv[1];\nif (isDirectExecution) {\n  startUltraThinRadarServer().catch((error) => {\n    const message = error instanceof Error ? error.message : String(error);\n    process.stderr.write(`[MCP] Fatal error: ${message}\n`);\n    process.exitCode = 1;\n  });\n}\n\nexports.StdioIdleGuard = StdioIdleGuard;\nexports.attachGracefulShutdown = attachGracefulShutdown;\nexports.startUltraThinRadarServer = startUltraThinRadarServer;\n"
  },
  "content_fetch_notes": []
}
